# Skill: Database Rules
- **PostgreSQL**: Primary data store.
- **UUIDs**: Use UUIDv4 for all primary keys.
- **Naming conventions**: snake_case for tables and columns.
- **Soft Delete**: Implement `deleted_at` instead of hard deletes.
- **Audit Logs**: Track who created/updated records.
