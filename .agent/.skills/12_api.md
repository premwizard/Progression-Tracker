# Skill: API Rules
- **REST Conventions**: Use standard HTTP methods.
- **Naming**: Plural nouns for resources.
- **Pagination & Filtering**: Standardize query parameters.
- **Versioning**: Prefix endpoints with `/api/v1/`.
